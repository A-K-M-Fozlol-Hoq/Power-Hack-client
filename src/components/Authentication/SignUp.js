import React from 'react';

const SignUp = (props) => {
  const { showLoginComponent, setShowLoginComponent } = props;
  return (
    <div>
      <h1>Signup</h1>
    </div>
  );
};

export default SignUp;
